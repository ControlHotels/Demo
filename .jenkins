pipeline {
    agent any
    stages {
        stage('Build') {
            steps {			
                    echo "Multiline shell steps works too"
            }
        }
		
		stage('Build1') {
            steps {			
                    echo "Multiline shell steps works too"
            }
        }
    }
}
