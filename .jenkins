pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
				C:\Windows\System32\bash.exe
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}
